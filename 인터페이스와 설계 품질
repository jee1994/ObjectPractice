디미터의 법칙
  협력 경로를 제한하라. (오직 하나의 도트만 사용하라) 
  this 객체, 매세더의 매개변수, this의 속성, this의 속성인 컬렉션의 요소, 매서드 내에서 생성된 지역객체
  외부에 딱 필요한 만큼만 보여줘야한다. 
  screening.getMovie().getDiscount() <- wrong implementation. 
  내부의 함수를 부르면 안된다. 
묻지 말고 시켜라 
  메세지 전송자는 수신자의 상태에 따라 결정을 내려서는 안된다. 그냥 무조건 보내고 땡.
  객체 외부에서 내부의 상태를 아는 것은 캡슐화에 위반된다. 
  메서드 이름을 지을때 어떻게 하는지를 드러내는것이 아니라 무엇을 하는지를 드러내야한다. 
의도를 드러내는 인터페이스 
명령-쿼리 분리
