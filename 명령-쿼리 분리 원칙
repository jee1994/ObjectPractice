명령은 객체의 정보를 수정하는 항수이고, 쿼리은 객체에 대한 정보를 가져오는 함수이다. 

이 명령과 쿼리가 한 함수에 있을때 문제가 될 수 있는데, 외부에서는 명령과 쿼리중 하나만 하는 함수로 보여지고 있는데 실제로 두가지 작업을 핟다고 하면 프로그래머들이 

디버깅 할때 예상하지 못하기 때문에 문제점을 발견하기 더 어려워진다. 

그래서 명령함수와 쿼리함수는 따로 구현해서 인터페이스에서 호출하게 하는것이 맞다. 
